'use strict';

import { PropTypes } from 'react';
import { requireNativeComponent, View } from 'react-native';

var iface = {
    name: 'YLLoading',
    propTypes: {
        ...View.propTypes,// 包含默认的View的属性
        isShow: PropTypes.bool
    },
};
module.exports = requireNativeComponent('RCTYLLoading', iface);